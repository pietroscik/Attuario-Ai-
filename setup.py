from setuptools import setup, find_packages

setup(
    name="attuario_ai",
    version="0.1.0",
    packages=find_packages(),
    install_requires=[],  # oppure carica da requirements.txt se necessario
)
